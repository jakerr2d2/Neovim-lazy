[tool.poetry]
name = "nvim"
version = "0.1.0"
description = ""
authors = ["jakerr2d2 <jakersport2021@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pynvim = "^0.5.0"

[tool.poetry.group.dev.dependencies]
neovim = "^0.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
